import { TestBed } from '@angular/core/testing';

import { CachedReadableService } from './cached-readable.service';

describe('CachedReadableService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

});
